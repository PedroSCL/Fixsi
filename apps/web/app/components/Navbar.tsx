"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { Menu, Search, UserRound, X } from "lucide-react";
import { useRouter } from "next/navigation";

export function Navbar(){const[user,setUser]=useState<{name:string}|null>(null),[search,setSearch]=useState(""),[open,setOpen]=useState(false),router=useRouter();useEffect(()=>{const stored=localStorage.getItem("fixsi_user");if(stored)setUser(JSON.parse(stored));},[]);function submit(e:React.FormEvent){e.preventDefault();if(search.trim()){router.push(`/services?search=${encodeURIComponent(search)}`);setOpen(false)}}const links=<><Link href="/#como-funciona" onClick={()=>setOpen(false)} className="font-bold text-slate-700 hover:text-orange-600">Como funciona</Link><Link href="/services" onClick={()=>setOpen(false)} className="font-bold text-slate-700 hover:text-orange-600">Serviços</Link></>;return <nav className="sticky top-0 z-30 border-t-2 border-orange-500 border-b border-slate-100 bg-white/95 px-5 py-1 backdrop-blur-md"><div className="mx-auto flex max-w-7xl items-center gap-5"><Link href="/" className="flex h-16 w-40 shrink-0 items-center overflow-visible"><img src="/img/logofixsi.png" alt="Fixsi" className="h-20 w-auto max-w-none object-contain"/></Link><div className="ml-auto hidden items-center gap-7 lg:flex">{links}</div><form onSubmit={submit} className="relative hidden w-full max-w-[285px] md:block"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-orange-500" size={18}/><input type="search" placeholder="O que você procura?" value={search} onChange={e=>setSearch(e.target.value)} className="w-full rounded-full border-2 border-orange-200 bg-orange-50/60 py-2.5 pl-11 pr-12 text-sm font-semibold text-slate-700 shadow-sm outline-none transition focus:border-orange-500 focus:bg-white focus:ring-4 focus:ring-orange-100"/><button type="submit" aria-label="Pesquisar" className="absolute right-1.5 top-1/2 flex h-8 w-8 -translate-y-1/2 items-center justify-center rounded-full bg-orange-500 text-white hover:bg-orange-600"><Search size={15}/></button></form>{user?<Link href="/dashboard" title="Minha conta" className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full border-2 border-[#1E3A5F] bg-white font-bold text-[#1E3A5F]">{user.name?.[0]?.toUpperCase()||"U"}</Link>:<Link href="/login" title="Entrar" className="rounded-full p-2 text-[#1E3A5F] hover:bg-orange-50"><UserRound size={26}/></Link>}<button onClick={()=>setOpen(!open)} className="rounded-lg p-2 text-[#1E3A5F] hover:bg-orange-50 lg:hidden" aria-label="Abrir menu">{open?<X/>:<Menu/>}</button></div>{open&&<div className="mx-auto flex max-w-7xl flex-col gap-4 border-t border-slate-100 pt-4 pb-2 lg:hidden"><form onSubmit={submit} className="relative md:hidden"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-orange-500" size={16}/><input type="search" placeholder="O que você procura?" value={search} onChange={e=>setSearch(e.target.value)} className="w-full rounded-xl border-2 border-orange-200 bg-orange-50 py-3 pl-9 pr-4 outline-none"/></form>{links}</div>}</nav>}
