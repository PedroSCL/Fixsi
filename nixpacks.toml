[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --prod=false"]

[phases.build]
cmds = ["cd packages/database && npx prisma generate && cd ../.. && cd apps/api && pnpm build"]

[start]
cmd = "cd apps/api && node dist/server.js"